// Generated by scripts/generate_example_catalog.py. Do not edit by hand.
import type { ExampleCatalogEntry } from "./index";

export const EXAMPLE_CATALOG = [
  {
    "id": "beam",
    "title": "Simply supported beam",
    "filename": "Beam.py",
    "domain": "Structural mechanics",
    "inputDimension": 4,
    "outputDimension": 1,
    "summary": "Beam deflection under uncertain geometry, material stiffness, and loading.",
    "difficulty": "introductory",
    "suggestedAnalyses": [
      "monte_carlo",
      "eda",
      "sobol"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\ndef function_of_interest(X):\n    E, F, L, I = X\n    Y = F * L**3 / (3 * E * I)\n    return [Y]\n\nmodel = ot.PythonFunction(4, 1, function_of_interest)\n\n# Define distributions with corrected descriptions\nE = ot.Beta(0.9, 3.1, 2.8e7, 4.8e7)\nE.setDescription([\"E\"])\n\nF = ot.LogNormal()\nF.setParameter(ot.LogNormalMuSigma()([3.0e4, 9.0e3, 15.0e3]))\nF.setDescription([\"F\"])\n\nL = ot.Uniform(250.0, 260.0)\nL.setDescription([\"L\"])\n\nI = ot.Beta(2.5, 4.0, 310.0, 450.0)\nI.setDescription([\"I\"])\n\n# Define correlation matrix for dependent variables\nR = ot.CorrelationMatrix(4)\nR[2, 3] = -0.2  # Assuming L and I are correlated with a Spearman correlation of -0.2\n\n# Define copula based on correlation matrix\ncopula = ot.NormalCopula(R)\n\n# Define joint distribution with copula\nproblem = ot.JointDistribution([E, F, L, I], copula)\n",
    "sha256": "52ee1acefacee4a4e4ba8387c774356a84ee3246090e97de56273e9b05d31aff"
  },
  {
    "id": "bike_speed",
    "title": "Cycling speed",
    "filename": "Bike_Speed.py",
    "domain": "Transport dynamics",
    "inputDimension": 8,
    "outputDimension": 1,
    "summary": "Cycling-speed response under rider, machine, and environmental uncertainty.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "morris",
      "sobol"
    ],
    "source": "import openturns as ot\nimport numpy as np\nfrom scipy.optimize import fsolve\n\n# Define the function that represents the non-linear equation\ndef bike_speed_eq(speed, *params):\n    rider_power, air_density, drag_coefficient, frontal_area, rolling_resistance, bike_mass, gravity_earth = params\n    return rider_power - (\n        0.5 * air_density * drag_coefficient * frontal_area * speed**3 +\n        rolling_resistance * bike_mass * gravity_earth * speed\n    )\n\n# Wrapper function to solve for the speed\ndef function_of_interest(inputs):\n    tire_radius, bike_mass, rider_power, air_density, rolling_resistance, drag_coefficient, gravity_earth, frontal_area = inputs\n    initial_guess = 10  # Initial guess for the speed (m/s)\n    params = (rider_power, air_density, drag_coefficient, frontal_area, rolling_resistance, bike_mass, gravity_earth)\n    speed_solution = fsolve(bike_speed_eq, initial_guess, args=params)\n    return [speed_solution[0]]\n\nmodel = ot.PythonFunction(8, 1, function_of_interest)\n\n# Problem definition\n# Define distributions with corrected descriptions\ntire_radius = ot.Uniform(0.3, 0.7)\ntire_radius.setDescription([\"tire_radius\"])\n\nbike_mass = ot.Uniform(5.0, 15.0)\nbike_mass.setDescription([\"bike_mass\"])\n\nrider_power = ot.Uniform(150.0, 400.0)\nrider_power.setDescription([\"rider_power\"])\n\nair_density = ot.Uniform(1.18, 1.3)\nair_density.setDescription([\"air_density\"])\n\nrolling_resistance = ot.Uniform(0.002, 0.005)\nrolling_resistance.setDescription([\"rolling_resistance\"])\n\ndrag_coefficient = ot.Uniform(0.7, 1.0)\ndrag_coefficient.setDescription([\"drag_coefficient\"])\n\ngravity_earth = ot.Uniform(9.78, 9.82)\ngravity_earth.setDescription([\"gravity_earth\"])\n\nfrontal_area = ot.Uniform(0.3, 0.6)\nfrontal_area.setDescription([\"frontal_area\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    tire_radius,\n    bike_mass,\n    rider_power,\n    air_density,\n    rolling_resistance,\n    drag_coefficient,\n    gravity_earth,\n    frontal_area\n])\n",
    "sha256": "855cd2d59ffa2302eef3cef2d325c49b8d7ed55666dc28de84c4a777284610c7"
  },
  {
    "id": "borehole",
    "title": "Borehole flow",
    "filename": "Borehole.py",
    "domain": "Hydrology",
    "inputDimension": 8,
    "outputDimension": 1,
    "summary": "Classical groundwater-flow benchmark with eight uncertain physical inputs.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "morris",
      "sobol",
      "pce"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Borehole Model Function\ndef function_of_interest(X):\n    rw, r, Tu, Hu, Tl, Hl, L, Kw = X\n    # rw  # Radius of borehole (m)\n    # r  # Radius of influence (m)\n    # Tu   # Transmissivity of upper aquifer (m²/year)\n    # Hu  # Potentiometric head of upper aquifer (m)\n    # Tl  # Transmissivity of lower aquifer (m²/year)\n    # Hl # Potentiometric head of lower aquifer (m)\n    # L   # Length of borehole (m)\n    # Kw  # Hydraulic conductivity of borehole (m/year)\n\n    # Compute intermediate terms\n    log_r_rw = np.log(r / rw)\n    numerator = 2 * np.pi * Tu * (Hu - Hl)\n    denominator = log_r_rw * (1 + (2 * L * Tu) / (log_r_rw * rw**2 * Kw) + Tu / Tl)\n    \n    # Calculate water flow rate through the borehole\n    Y = numerator / denominator  # Flow rate (m³/year)\n\n    return [Y]\n\nmodel = ot.PythonFunction(8, 1, function_of_interest)\n\n# Problem definition for the Borehole Model\n# Define distributions with corrected descriptions\nrw = ot.Normal(0.10, 0.0161812)\nrw.setDescription([\"rw\"])\n\nr = ot.LogNormal(7.71, 1.0056)\nr.setDescription([\"r\"])\n\nTu = ot.Uniform(63070, 115600)\nTu.setDescription([\"Tu\"])\n\nHu = ot.Uniform(990, 1110)\nHu.setDescription([\"Hu\"])\n\nTl = ot.Uniform(63.1, 116)\nTl.setDescription([\"Tl\"])\n\nHl = ot.Uniform(700, 820)\nHl.setDescription([\"Hl\"])\n\nL = ot.Uniform(1120, 1680)\nL.setDescription([\"L\"])\n\nKw = ot.Uniform(9855, 12045)\nKw.setDescription([\"Kw\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    rw,\n    r,\n    Tu,\n    Hu,\n    Tl,\n    Hl,\n    L,\n    Kw\n])",
    "sha256": "3d41bbb1e792ef582faa14df875abe252739d7b679dc4b811676711c6917ea6e"
  },
  {
    "id": "chaboche_model",
    "title": "Chaboche material model",
    "filename": "Chaboche_Model.py",
    "domain": "Material mechanics",
    "inputDimension": 4,
    "outputDimension": 1,
    "summary": "Nonlinear material-response benchmark based on a Chaboche constitutive relation.",
    "difficulty": "advanced",
    "suggestedAnalyses": [
      "monte_carlo",
      "sobol",
      "gpr"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# The Chaboche mechanical model of a stress-strain curve\ndef function_of_interest(X):\n    Strain, R, C, Gamma = X\n    R *= 1.0e6  # Scale R into MPa\n    C *= 1.0e6  # Scale C into MPa\n    sigma = R - C * np.expm1(-Gamma * Strain) / Gamma\n    sigma /= 1.0e6  # Un-scale sigma from MPa\n    return [sigma]\n\nmodel = ot.PythonFunction(4, 1, function_of_interest)\n\n# Problem definition for the Chaboche Model\n# Define distributions with corrected descriptions\nStrain = ot.Uniform(0.0, 0.07)\nStrain.setDescription([\"Strain\"])\n\nR = ot.LogNormal()\nR.setParameter(ot.LogNormalMuSigma()([750.0, 11.0, 0.0]))\nR.setDescription([\"R\"])\n\nC = ot.Normal(2750.0, 250.0)\nC.setDescription([\"C\"])\n\nGamma = ot.Normal(10.0, 2.0)\nGamma.setDescription([\"Gamma\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    Strain,\n    R,\n    C,\n    Gamma\n])\n",
    "sha256": "22f8114a18d3cce36b6437e5015e071b6ebacd23e6867175936d9461dd936aa4"
  },
  {
    "id": "chemical_reactor",
    "title": "Chemical reactor",
    "filename": "Chemical_Reactor.py",
    "domain": "Process engineering",
    "inputDimension": 6,
    "outputDimension": 1,
    "summary": "Reactor-response model with uncertain kinetic and operating inputs.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "morris",
      "reliability"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Chemical Reactor Function\ndef function_of_interest(X):\n    k0, E, R, T, CA0, V = X  # Arrhenius parameters and reactor conditions\n    # Calculate the rate constant using Arrhenius equation\n    k = k0 * np.exp(-E / (R * T))\n    # Assume first-order reaction: A → Products\n    # Use the design equation for a continuous stirred-tank reactor (CSTR)\n    Q = 1.0  # Flow rate in m³/s\n    CA = CA0 / (1 + k * V / Q)\n    # Conversion X = (CA0 - CA) / CA0\n    conversion = (CA0 - CA) / CA0\n    return [conversion]\n\nmodel = ot.PythonFunction(6, 1, function_of_interest)\n\n# Problem definition for the Chemical Reactor\n# Define distributions with corrected descriptions\nk0 = ot.LogNormal()\nk0.setParameter(ot.LogNormalMuSigma()([1e10, 1e9, 0]))\nk0.setDescription([\"k0\"])\n\nE = ot.Uniform(80000, 120000)\nE.setDescription([\"E\"])\n\nR = ot.Uniform(8.314, 8.514)\nR.setDescription([\"R\"])\n\nT = ot.Normal(350, 5)\nT.setDescription([\"T\"])\n\nCA0 = ot.Uniform(1.0, 2.0)\nCA0.setDescription([\"CA0\"])\n\nV = ot.Uniform(1.0, 5.0)\nV.setDescription([\"V\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    k0,\n    E,\n    R,\n    T,\n    CA0,\n    V\n])\n",
    "sha256": "be9646f8f8c0d3985efd46c10aec6cc6d4a102166cd7068b7bc54c9425122831"
  },
  {
    "id": "cylinder_heating",
    "title": "Cylinder heating",
    "filename": "Cylinder_heating.py",
    "domain": "Thermal engineering",
    "inputDimension": 5,
    "outputDimension": 1,
    "summary": "Transient heating response for a cylinder with uncertain thermal properties.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "taylor",
      "pce"
    ],
    "source": "import openturns as ot\nimport numpy as np\nfrom scipy.integrate import solve_ivp\n\n# Define the function of interest for the heat transfer model\ndef function_of_interest(X):\n    radius, length, k, h, Q = X  # Inputs: radius, length, thermal conductivity, convective heat transfer coefficient, heat generation rate\n\n    # Adjust heat generation rate by volume according to the length\n    Q_adjusted = Q / length\n\n    # Differential equation for radial temperature distribution\n    def heat_eq(r, T):\n        # Here, T[0] is the temperature and T[1] is its radial derivative dT/dr\n        dTdr = T[1]\n        d2Tdr2 = -h * (T[0] - 300) / (k * r) + Q_adjusted / k  # 300 K as the boundary surface temperature\n        return [dTdr, d2Tdr2]\n\n    # Initial conditions at the center (r=0 can be singular, so start slightly off-center)\n    r_min = 1e-5  # start slightly off-center to avoid singularity at r=0\n    initial_conditions = [300, 0]  # assume the center starts at the boundary temperature, gradient is zero\n\n    # Solve the differential equation from near the center of the cylinder to its surface\n    sol = solve_ivp(heat_eq, [r_min, radius], initial_conditions, method='BDF')\n\n    # Return the maximum temperature, which is typically at the center\n    return [max(sol.y[0])]\n\nmodel = ot.PythonFunction(5, 1, function_of_interest)\n\n# Problem definition for the thermal model\n# Define distributions with corrected descriptions\nradius = ot.Uniform(0.01, 0.1)      # radius in meters\nradius.setDescription([\"radius\"])\n\nlength = ot.Uniform(0.1, 1.0)       # length in meters\nlength.setDescription([\"length\"])\n\nk = ot.Uniform(15, 50)              # thermal conductivity in W/(m·K)\nk.setDescription([\"k\"])\n\nh = ot.Uniform(5, 25)               # convective heat transfer coefficient in W/(m²·K)\nh.setDescription([\"h\"])\n\nQ = ot.Uniform(1000, 5000)          # heat generation rate per volume in W/m³\nQ.setDescription([\"Q\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    radius,\n    length,\n    k,\n    h,\n    Q\n])\n",
    "sha256": "e1f3b642ff7bfa3b3cf9cbc75c8c6221a48a76611e990b6640b73d7ba297db16"
  },
  {
    "id": "damped_oscillator",
    "title": "Damped oscillator",
    "filename": "Damped_Oscillator.py",
    "domain": "Structural dynamics",
    "inputDimension": 8,
    "outputDimension": 1,
    "summary": "Dynamic oscillator response with uncertain mass, damping, forcing, and stiffness.",
    "difficulty": "advanced",
    "suggestedAnalyses": [
      "monte_carlo",
      "morris",
      "gpr",
      "reliability"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Damped Oscillator Performance Function\n\ndef function_of_interest(X):\n    p = 3  # Safety factor parameter\n\n    # Extract input variables\n    m_p, m_s, k_p, k_s, zeta_p, zeta_s, S_0, F_s = X\n\n    # Compute natural frequencies\n    omega_p = np.sqrt(k_p / m_p)  # Primary natural frequency (rad/s)\n    omega_s = np.sqrt(k_s / m_s)  # Secondary natural frequency (rad/s)\n\n    # Compute other parameters\n    gamma = m_s / m_p                     # Mass ratio\n    omega_a = (omega_p + omega_s) / 2     # Average natural frequency\n    zeta_a = (zeta_p + zeta_s) / 2        # Average damping ratio\n    theta = (omega_p - omega_s) / omega_a # Tuning parameter\n\n    # Compute mean-square relative displacement\n    numerator = np.pi * S_0 / (4 * zeta_s * omega_s**3)\n    denominator = (zeta_p * zeta_s * (4 * zeta_a**2 + theta**2) + gamma * zeta_a**2)\n    factor = (zeta_a * zeta_s) / denominator\n    term = (zeta_p * omega_p**3 + zeta_s * omega_s**3) * omega_p / (4 * zeta_a * omega_a**4)\n    mean_square_displacement = numerator * factor * term\n\n    # Evaluate the performance function\n    g = F_s - p * k_s * np.sqrt(mean_square_displacement)\n\n    return [g]\n\nmodel = ot.PythonFunction(8, 1, function_of_interest)\n\n# Problem definition for the Damped Oscillator Model\n# Create LogNormal distributions using the original parameters\n# Convert from mu_log, sigma_log to the appropriate OpenTURNS parameters\n\n# m_p: Primary mass\nm_p = ot.LogNormal()\nmu_log = 0.4004899\nsigma_log = 0.0997513\nmu = np.exp(mu_log + sigma_log**2/2)  # Convert to mean of LogNormal\nsigma = mu * np.sqrt(np.exp(sigma_log**2) - 1)  # Convert to std of LogNormal\nm_p.setParameter(ot.LogNormalMuSigma()([mu, sigma, 0]))\nm_p.setDescription([\"m_p\"])\n\n# m_s: Secondary mass\nm_s = ot.LogNormal()\nmu_log = -4.6101453\nsigma_log = 0.0997513\nmu = np.exp(mu_log + sigma_log**2/2)  # Convert to mean of LogNormal\nsigma = mu * np.sqrt(np.exp(sigma_log**2) - 1)  # Convert to std of LogNormal\nm_s.setParameter(ot.LogNormalMuSigma()([mu, sigma, 0]))\nm_s.setDescription([\"m_s\"])\n\n# k_p: Primary stiffness\nk_p = ot.LogNormal()\nmu_log = -0.0196103\nsigma_log = 0.1980422\nmu = np.exp(mu_log + sigma_log**2/2)  # Convert to mean of LogNormal\nsigma = mu * np.sqrt(np.exp(sigma_log**2) - 1)  # Convert to std of LogNormal\nk_p.setParameter(ot.LogNormalMuSigma()([mu, sigma, 0]))\nk_p.setDescription([\"k_p\"])\n\n# k_s: Secondary stiffness\nk_s = ot.LogNormal()\nmu_log = -4.6247805\nsigma_log = 0.1980422\nmu = np.exp(mu_log + sigma_log**2/2)  # Convert to mean of LogNormal\nsigma = mu * np.sqrt(np.exp(sigma_log**2) - 1)  # Convert to std of LogNormal\nk_s.setParameter(ot.LogNormalMuSigma()([mu, sigma, 0]))\nk_s.setDescription([\"k_s\"])\n\n# zeta_p: Primary damping ratio\nzeta_p = ot.LogNormal()\nmu_log = -3.069942\nsigma_log = 0.3856625\nmu = np.exp(mu_log + sigma_log**2/2)  # Convert to mean of LogNormal\nsigma = mu * np.sqrt(np.exp(sigma_log**2) - 1)  # Convert to std of LogNormal\nzeta_p.setParameter(ot.LogNormalMuSigma()([mu, sigma, 0]))\nzeta_p.setDescription([\"zeta_p\"])\n\n# zeta_s: Secondary damping ratio\nzeta_s = ot.LogNormal()\nmu_log = -4.023594\nsigma_log = 0.4723807\nmu = np.exp(mu_log + sigma_log**2/2)  # Convert to mean of LogNormal\nsigma = mu * np.sqrt(np.exp(sigma_log**2) - 1)  # Convert to std of LogNormal\nzeta_s.setParameter(ot.LogNormalMuSigma()([mu, sigma, 0]))\nzeta_s.setDescription([\"zeta_s\"])\n\n# S_0: Excitation intensity\nS_0 = ot.LogNormal()\nmu_log = 4.600195\nsigma_log = 0.0997513\nmu = np.exp(mu_log + sigma_log**2/2)  # Convert to mean of LogNormal\nsigma = mu * np.sqrt(np.exp(sigma_log**2) - 1)  # Convert to std of LogNormal\nS_0.setParameter(ot.LogNormalMuSigma()([mu, sigma, 0]))\nS_0.setDescription([\"S_0\"])\n\n# F_s: Force capacity\nF_s = ot.LogNormal()\nmu_log = 2.703075\nsigma_log = 0.0997513\nmu = np.exp(mu_log + sigma_log**2/2)  # Convert to mean of LogNormal\nsigma = mu * np.sqrt(np.exp(sigma_log**2) - 1)  # Convert to std of LogNormal\nF_s.setParameter(ot.LogNormalMuSigma()([mu, sigma, 0]))\nF_s.setDescription([\"F_s\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    m_p,\n    m_s,\n    k_p,\n    k_s,\n    zeta_p,\n    zeta_s,\n    S_0,\n    F_s\n])\n",
    "sha256": "2b2de51930465042ccc77813f78dd2b3f2c7c449fa9b91267e4deaf4db72e23a"
  },
  {
    "id": "epidemic_model",
    "title": "Epidemic response",
    "filename": "Epidemic_Model.py",
    "domain": "Population dynamics",
    "inputDimension": 7,
    "outputDimension": 1,
    "summary": "Compartmental epidemic-response benchmark with uncertain transition parameters.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "morris",
      "sobol"
    ],
    "source": "import openturns as ot\nimport numpy as np\nfrom scipy.integrate import solve_ivp\n\n# SIR Model Function\ndef function_of_interest(X):\n    S0, I0, R0, beta, gamma, N, t_max = X\n\n    # Define the SIR model differential equations\n    def sir_model(t, y):\n        S, I, R = y\n        dSdt = -beta * S * I / N\n        dIdt = beta * S * I / N - gamma * I\n        dRdt = gamma * I\n        return [dSdt, dIdt, dRdt]\n\n    # Initial conditions vector\n    y0 = [S0, I0, R0]\n\n    # Time points\n    t_span = [0, t_max]\n    t_eval = np.linspace(0, t_max, 100)\n\n    # Solve the differential equations\n    sol = solve_ivp(sir_model, t_span, y0, t_eval=t_eval)\n\n    # Total number of infected individuals over time (max I)\n    total_infected = max(sol.y[1])\n\n    return [total_infected]\n\nmodel = ot.PythonFunction(7, 1, function_of_interest)\n\n# Problem definition for the Epidemic Model\n# Define distributions with corrected descriptions\nS0 = ot.Uniform(990, 1000)       # S0: Initial susceptible population\nS0.setDescription([\"S0\"])\n\nI0 = ot.Uniform(1, 10)           # I0: Initial infected population\nI0.setDescription([\"I0\"])\n\nR0 = ot.Uniform(1, 5)            # R0: Initial recovered population\nR0.setDescription([\"R0\"])\n\nbeta = ot.Uniform(0.1, 0.5)      # beta: Transmission rate\nbeta.setDescription([\"beta\"])\n\ngamma = ot.Uniform(0.05, 0.2)    # gamma: Recovery rate\ngamma.setDescription([\"gamma\"])\n\nN = ot.Uniform(1000, 1010)       # N: Total population\nN.setDescription([\"N\"])\n\nt_max = ot.Uniform(30, 60)       # t_max: Simulation time (days)\nt_max.setDescription([\"t_max\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    S0,\n    I0,\n    R0,\n    beta,\n    gamma,\n    N,\n    t_max\n])\n",
    "sha256": "b81730c768465c89c17f179b7859f0558f56d3a5f4d6bd62373f4450d85efab8"
  },
  {
    "id": "flood_model",
    "title": "Flood model",
    "filename": "Flood_Model.py",
    "domain": "Flood risk",
    "inputDimension": 8,
    "outputDimension": 1,
    "summary": "River-overflow benchmark linking hydrological and geometric uncertainty.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "sobol",
      "reliability"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\ndef function_of_interest(X):\n    # Inputs:\n    # Q: Flow rate\n    # Ks: Strickler coefficient\n    # Zv: Downstream height\n    # Zm: Upstream height\n    # B: River width\n    # L: River length\n    # Zb: River bank altitude\n    # Hd: Dyke height\n    Q, Ks, Zv, Zm, B, L, Zb, Hd = X\n    \n    # 1. Calculate the slope alpha\n    # Use max(0, ...) to ensure no domain errors if sample makes Zv > Zm\n    alpha = (Zm - Zv) / L if Zm >= Zv else 0.0\n    \n    # 2. Calculate the water depth H using Manning-Strickler\n    if Ks > 0 and Q > 0 and alpha > 0:\n        H = (Q / (Ks * B * np.sqrt(alpha))) ** 0.6\n    else:\n        H = 0.0\n    \n    # 3. Calculate flood altitude Zc and dyke altitude Zd\n    Zc = H + Zv\n    Zd = Zb + Hd\n    \n    # 4. Determine Overflow S\n    # S > 0 means the water is higher than the dyke (FLOOD)\n    S = Zc - Zd\n    \n    return [S]\n\nmodel = ot.PythonFunction(8, 1, function_of_interest)\n\n# Problem definition\n# ------------------\n\n# Q: Gumbel(scale=558, mode=1013)\n# Note: OpenTURNS constructor is Gumbel(scale, mode)\nQ = ot.Gumbel(558.0, 1013.0)\nQ.setDescription([\"Q (Flow Rate)\"])\n\n# Ks: Normal(mu=30, sigma=7.5)\nKs = ot.Normal(30.0, 7.5)\nKs.setDescription([\"Ks (Strickler)\"])\n\n# Zv: Uniform(49, 51)\nZv = ot.Uniform(49.0, 51.0)\nZv.setDescription([\"Zv (Downstream)\"])\n\n# Zm: Uniform(54, 56)\nZm = ot.Uniform(54.0, 56.0)\nZm.setDescription([\"Zm (Upstream)\"])\n\n# B: Triangular(min=295, mode=300, max=305)\nB = ot.Triangular(295.0, 300.0, 305.0)\nB.setDescription([\"B (Width)\"])\n\n# L: Triangular(min=4990, mode=5000, max=5010)\nL = ot.Triangular(4990.0, 5000.0, 5010.0)\nL.setDescription([\"L (Length)\"])\n\n# Zb: Triangular(min=55, mode=55.5, max=56)\nZb = ot.Triangular(55.0, 55.5, 56.0)\nZb.setDescription([\"Zb (Bank Alt)\"])\n\n# Hd: Uniform(2, 4) - \"Low Dyke\" scenario\nHd = ot.Uniform(2.0, 4.0)\nHd.setDescription([\"Hd (Dyke Height)\"])\n\n# Define joint distribution (Independent inputs)\nmarginals = [Q, Ks, Zv, Zm, B, L, Zb, Hd]\nproblem = ot.JointDistribution(marginals)",
    "sha256": "03dfc3d0b0d4511b1d8134a5fd9adf252a8030b7a19e433503a456cdff2454b0"
  },
  {
    "id": "ishigami",
    "title": "Ishigami function",
    "filename": "Ishigami.py",
    "domain": "Sensitivity benchmark",
    "inputDimension": 3,
    "outputDimension": 1,
    "summary": "Nonlinear, non-monotonic benchmark with a strong input interaction.",
    "difficulty": "introductory",
    "suggestedAnalyses": [
      "monte_carlo",
      "eda",
      "sobol",
      "fast",
      "hsic"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Ishigami Function\ndef function_of_interest(X):\n    x1, x2, x3 = X\n    Y = np.sin(x1) + 7 * np.sin(x2) ** 2 + 0.1 * x3 ** 4 * np.sin(x1)\n    return [Y]\n\nmodel = ot.PythonFunction(3, 1, function_of_interest)\n\n# Problem definition for the Ishigami Function\n# Define distributions with corrected descriptions\nx1 = ot.Uniform(-np.pi, np.pi)\nx1.setDescription([\"x1\"])\n\nx2 = ot.Uniform(-np.pi, np.pi)\nx2.setDescription([\"x2\"])\n\nx3 = ot.Uniform(-np.pi, np.pi)\nx3.setDescription([\"x3\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    x1,\n    x2,\n    x3\n])\n",
    "sha256": "f1f0db95159f00b08718a639a6a8a0d72f51320c728897813d1914ac78494d38"
  },
  {
    "id": "logistic_model",
    "title": "Logistic growth",
    "filename": "Logistic_Model.py",
    "domain": "Population dynamics",
    "inputDimension": 3,
    "outputDimension": 1,
    "summary": "Logistic growth response with uncertain growth and capacity parameters.",
    "difficulty": "introductory",
    "suggestedAnalyses": [
      "monte_carlo",
      "taylor",
      "pce"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# The logistic model of population growth\ndef function_of_interest(X):\n    y0, a, c = X\n    t0 = 0.0  # The initial time (years)\n    t = 2000.0  # The final time (years)\n    y0 *= 1.0e6  # Convert into millions\n    b = np.exp(c)\n    y = a * y0 / (b * y0 + (a - b * y0) * np.exp(-a * (t - t0)))\n    y /= 1.0e6  # Convert from millions\n    return [y]\n\nmodel = ot.PythonFunction(3, 1, function_of_interest)\n\n# Problem definition for the Logistic Model\n# Define distributions with corrected descriptions\nY0 = ot.Normal(3.9, 1.0)         # Y0: Initial population (millions)\nY0.setDescription([\"Y0\"])\n\nA = ot.Normal(0.03, 0.01)        # A\nA.setDescription([\"A\"])\n\nC = ot.Normal(-22.6, 0.2)        # C\nC.setDescription([\"C\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    Y0,\n    A,\n    C\n])\n",
    "sha256": "ec56238ecb1dd823f6b012cea6779b124e766aeec322f16eb36ac861c3f2e44b"
  },
  {
    "id": "material_stress",
    "title": "Material stress",
    "filename": "Material_Stress.py",
    "domain": "Material mechanics",
    "inputDimension": 5,
    "outputDimension": 1,
    "summary": "Stress response under uncertain loading, geometry, and material properties.",
    "difficulty": "introductory",
    "suggestedAnalyses": [
      "monte_carlo",
      "sobol",
      "reliability"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Material Stress Function\ndef function_of_interest(X):\n    gamma, phi, Rs, G, M = X\n    b = 2.54e-9  # Constant value of b\n\n    # Ensure sqrt_term is always non-negative by taking the max with zero\n    sqrt_term = max((8.0 * gamma * phi * Rs) / (np.pi * G * pow(b, 2)), 0)\n\n    # Compute sigma_c ensuring the value is always physical\n    sigma_c = abs(((M * gamma) / (2.0 * b)) * (np.sqrt(sqrt_term) - phi) / 1e6)\n\n    # Avoid negative or zero sigma_c in MPa values by adding a small positive constant if needed\n    sigma_c = max(sigma_c, 1e-6)\n\n    return [sigma_c]\n\nmodel = ot.PythonFunction(5, 1, function_of_interest)\n\n# Problem definition for the Material Stress Model\n# Define distributions with corrected descriptions\ngamma = ot.Uniform(0.15, 0.25)\ngamma.setDescription([\"gamma\"])\n\nphi = ot.Uniform(0.30, 0.45)\nphi.setDescription([\"phi\"])\n\nRs = ot.Uniform(1e-8, 3e-8)\nRs.setDescription([\"Rs\"])\n\nG = ot.Uniform(6e10, 8e10)\nG.setDescription([\"G\"])\n\nM = ot.Normal(3.05, 0.15)\nM.setDescription([\"M\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    gamma,\n    phi,\n    Rs,\n    G,\n    M\n])\n",
    "sha256": "ce2b3e95991438d1907e46837519fa94129734293d1773a1441e233ef7add77e"
  },
  {
    "id": "morris_function",
    "title": "Morris 20-function",
    "filename": "Morris_Function.py",
    "domain": "Screening benchmark",
    "inputDimension": 20,
    "outputDimension": 1,
    "summary": "High-dimensional nonlinear benchmark designed for factor screening.",
    "difficulty": "advanced",
    "suggestedAnalyses": [
      "morris",
      "monte_carlo",
      "gpr"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Morris Function\ndef function_of_interest(X):\n    # Ensure X is a numpy array\n    X = np.asarray(X)\n\n    # Ensure that X has 20 variables\n    if X.shape[0] != 20:\n        raise ValueError(\"Expected 20 input variables.\")\n\n    U = X  # Input variables\n\n    # Compute transformed variables W\n    W = 2 * (U - 0.5)\n\n    # Adjust W at specific indices for nonlinearity\n    indices = [2, 4, 6]  # Corresponds to variables 3, 5, 7 in MATLAB (1-based indexing)\n    W[indices] = 2 * (1.1 * U[indices] / (U[indices] + 0.1) - 0.5)\n\n    # Initialize the output\n    Y = 0.0\n\n    # Linear terms\n    for i in range(20):\n        if i <= 9:  # First 10 variables\n            bi = 20\n        else:\n            bi = (-1) ** (i + 1)  # Adjust for zero-based indexing\n        Y += bi * W[i]\n\n    # Two-way interaction terms\n    for i in range(19):\n        for j in range(i + 1, 20):\n            if i <= 5 and j <= 5:  # First 6 variables\n                bij = -15\n            else:\n                bij = (-1) ** (i + j + 2)\n            Y += bij * W[i] * W[j]\n\n    # Three-way interaction terms\n    for i in range(18):\n        for j in range(i + 1, 19):\n            for k in range(j + 1, 20):\n                if i <= 4 and j <= 4 and k <= 4:  # First 5 variables\n                    bijl = -10\n                else:\n                    bijl = 0\n                Y += bijl * W[i] * W[j] * W[k]\n\n    # Four-way interaction term\n    bijls = 5\n    Y += bijls * W[0] * W[1] * W[2] * W[3]  # First four variables\n\n    return [Y]\n\nmodel = ot.PythonFunction(20, 1, function_of_interest)\n\n# Problem definition for the Morris Function\n# Define distributions with corrected descriptions\nnames = ['X' + str(i + 1) for i in range(20)]\ndistributions = [ot.Uniform(0, 1) for _ in range(20)]\nfor name, dist in zip(names, distributions):\n    dist.setDescription([name])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution(distributions)\n",
    "sha256": "11389923dba3ca438c9ad04eebbe4db12c505660be3d7dc5e728e9be6c5bb24f"
  },
  {
    "id": "portfolio_risk",
    "title": "Portfolio risk",
    "filename": "Portfolio_Risk.py",
    "domain": "Financial risk",
    "inputDimension": 4,
    "outputDimension": 1,
    "summary": "Portfolio outcome benchmark with uncertain returns and dependence parameters.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "eda",
      "reliability"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Portfolio Risk Function\ndef function_of_interest(X):\n    S0, mu, sigma, T = X  # Initial portfolio value, expected return, volatility, time horizon\n    N = 10                # Number of assets\n    rho = 0.5             # Correlation coefficient between assets (assumed constant)\n\n    np.random.seed(42)    # For reproducibility\n    dt = T / 252          # Time step (assuming 252 trading days in a year)\n\n    # Simulate correlated asset returns using Cholesky decomposition\n    mean_returns = np.full(N, mu * dt)\n    # Construct the covariance matrix with correlations\n    cov_matrix = sigma**2 * dt * (rho * np.ones((N, N)) + (1 - rho) * np.eye(N))\n    L = np.linalg.cholesky(cov_matrix)\n\n    # Generate random returns\n    Z = np.random.normal(size=(N,))\n    correlated_returns = mean_returns + L @ Z\n\n    # Calculate portfolio value\n    portfolio_value = S0 * np.exp(np.sum(correlated_returns))\n    return [portfolio_value]\n\nmodel = ot.PythonFunction(4, 1, function_of_interest)\n\n# Problem definition for the Portfolio Risk Model\n# Define distributions with corrected descriptions\nS0 = ot.Uniform(800000, 1200000)  # S0: $800,000 - $1,200,000\nS0.setDescription([\"S0\"])\n\n# Create Normal distributions with mean and standard deviation directly\nmu = ot.Normal(0.1, 0.02)  # mu: Expected annual return (10%)\nmu.setDescription([\"mu\"])\n\nsigma = ot.Normal(0.2, 0.05)  # sigma: Volatility (20%)\nsigma.setDescription([\"sigma\"])\n\nT = ot.Uniform(0.5, 1.5)          # T: 0.5 - 1.5 years\nT.setDescription([\"T\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    S0,\n    mu,\n    sigma,\n    T\n])\n",
    "sha256": "7230f14e47ef64118d06b4a97f35d0e64104fedf7588134c8aac58e2ac4da81c"
  },
  {
    "id": "rocket_trajectory",
    "title": "Rocket trajectory",
    "filename": "Rocket_Trajectory.py",
    "domain": "Aerospace",
    "inputDimension": 5,
    "outputDimension": 1,
    "summary": "Trajectory-response benchmark with uncertain propulsion and vehicle parameters.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "morris",
      "gpr"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Rocket Trajectory Function\ndef function_of_interest(X):\n    m0, mf, ve, theta, g = X  # Initial mass, fuel mass, exhaust velocity, launch angle, gravity\n    # Calculate the final velocity using Tsiolkovsky rocket equation\n    delta_v = ve * np.log(m0 / (m0 - mf))\n    # Calculate horizontal and vertical components\n    vx = delta_v * np.cos(np.radians(theta))\n    vy = delta_v * np.sin(np.radians(theta))\n    # Calculate maximum altitude (assuming vertical motion under gravity)\n    h_max = (vy ** 2) / (2 * g)\n    # Calculate horizontal range\n    range_ = (vx * vy) / g\n    return [h_max]\n\nmodel = ot.PythonFunction(5, 1, function_of_interest)\n\n# Problem definition for the Rocket Trajectory Model\n# Define distributions with corrected descriptions\nm0 = ot.Uniform(50000, 60000)    # m0: Initial mass (kg)\nm0.setDescription([\"m0\"])\n\nmf = ot.Uniform(10000, 20000)    # mf: Fuel mass (kg)\nmf.setDescription([\"mf\"])\n\nve = ot.Normal(3000, 100)  # ve: Exhaust velocity (m/s)\nve.setDescription([\"ve\"])\n\ntheta = ot.Uniform(80, 90)        # theta: Launch angle (degrees)\ntheta.setDescription([\"theta\"])\n\ng = ot.Uniform(9.8, 9.81)         # g: Gravity (m/s²)\ng.setDescription([\"g\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    m0,\n    mf,\n    ve,\n    theta,\n    g\n])\n",
    "sha256": "b3baa2f1ed8d126eae43cbda732db6fbef2aebcf54b8994c84a804f98d0a5457"
  },
  {
    "id": "solar_panel_output",
    "title": "Solar panel output",
    "filename": "Solar_Panel_Output.py",
    "domain": "Renewable energy",
    "inputDimension": 6,
    "outputDimension": 1,
    "summary": "Power-output response under uncertain irradiance, temperature, and component properties.",
    "difficulty": "introductory",
    "suggestedAnalyses": [
      "monte_carlo",
      "sobol",
      "pce"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Solar Panel Output Function\ndef function_of_interest(X):\n    G, A, eta, T_cell, T_ref, beta = X  # Solar irradiance, area, efficiency, cell temperature, reference temperature, temperature coefficient\n    # Adjust efficiency based on temperature\n    eta_adjusted = eta * (1 + beta * (T_cell - T_ref))\n    # Power output\n    P = G * A * eta_adjusted\n    return [P]\n\nmodel = ot.PythonFunction(6, 1, function_of_interest)\n\n# Problem definition for the Solar Panel Output Model\n# Define distributions with corrected descriptions\nG = ot.Uniform(800, 1000)      # G: Solar irradiance (W/m²)\nG.setDescription([\"G\"])\n\nA = ot.Uniform(1.5, 2.0)        # A: Panel area (m²)\nA.setDescription([\"A\"])\n\neta = ot.Uniform(0.15, 0.20)    # eta: Efficiency at T_ref (dimensionless)\neta.setDescription([\"eta\"])\n\nT_cell = ot.Normal(45, 5)       # T_cell: Cell temperature (°C)\nT_cell.setDescription([\"T_cell\"])\n\nT_ref = ot.Uniform(25, 30)      # T_ref: Reference temperature (°C)\nT_ref.setDescription([\"T_ref\"])\n\nbeta = ot.Uniform(-0.005, -0.003)  # beta: Temperature coefficient (1/°C)\nbeta.setDescription([\"beta\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    G,\n    A,\n    eta,\n    T_cell,\n    T_ref,\n    beta\n])\n",
    "sha256": "13c6aeeb268d473c968b6fd8643737495328e8b43888bd5f615efdae872caa83"
  },
  {
    "id": "stiffened_panel",
    "title": "Stiffened panel",
    "filename": "Stiffened_Panel.py",
    "domain": "Structural mechanics",
    "inputDimension": 10,
    "outputDimension": 1,
    "summary": "Structural-response benchmark for a stiffened panel with ten uncertain inputs.",
    "difficulty": "advanced",
    "suggestedAnalyses": [
      "morris",
      "monte_carlo",
      "gpr",
      "reliability"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n\"\"\"\nStiffened Panel Example for Reliability Analysis\n===============================================\n\nThis example models the critical shear force of a stiffened panel, which is a common\nstructural component in aerospace and marine engineering.\n\nImportant Notes:\n---------------\n1. Original output is in Newtons per meter (N/m), but for better usability in reliability\n   analysis, we scale it to MPa (mega pascals).\n2. Failure criterion: Output < Threshold (when the critical load is less than required)\n3. Recommended threshold value: 150-180 MPa\n4. All input dimensions are in meters unless otherwise noted\n\"\"\"\n\ndef function_of_interest(X):\n    \"\"\"\n    Calculate the critical shear force of a stiffened panel\n    \n    Parameters:\n    -----------\n    X : list or array with 10 elements\n        E    : Young's modulus (GPa)\n        nu   : Poisson's ratio\n        h_c  : Distance between mean surface of hat and foot of stiffener (m)\n        ell  : Length of stiffener flank (m)\n        f_1  : Width of stiffener foot (m)\n        f_2  : Width of stiffener hat (m)\n        t    : Thickness of panel and stiffener (m)\n        a    : Width of panel (m)\n        b_0  : Distance between two stiffeners (m)\n        p    : Half-width of stiffener (m)\n    \n    Returns:\n    --------\n    list with 1 element:\n        Critical shear force scaled to MPa for easier interpretation\n    \"\"\"\n    E, nu, h_c, ell, f_1, f_2, t, a, b_0, p = X\n    \n    # Calculate parameters\n    kxy = 5.35 + 4 * (b_0 / a) ** 2\n    D = E * t**3 / (12 * (1 - nu**2))\n    A = ell * t\n    A_bar = A + t * (p + (f_1 - f_2) / 2)\n    h_0 = (A * (h_c + 2 * t) + t**2 * (f_1 - f_2)) / (2 * A_bar)\n    h = h_c + t\n    factor = 1 + (2 * p * (h - 2 * h_0) - h_c * (f_1 - f_2)) / (4 * h_0 * ell)\n    \n    # Calculate critical shear force per unit length (N/m)\n    Nxy = kxy * np.pi**2 * D / b_0**2 * factor\n    \n    # Scale to get values around 200 MPa \n    # Using a much larger scaling factor (1e9) to get the desired range\n    Nxy_MPa = Nxy * 1e9\n    \n    return [Nxy_MPa]\n\n# Create OpenTURNS model\nmodel = ot.PythonFunction(10, 1, function_of_interest)\nmodel.setOutputDescription([\"Critical Shear Force (MPa)\"])\n\n# Problem definition for the Stiffened Panel Model\n# Define distributions with corrected descriptions\nE = ot.Normal(110.0, 0.05 * 110.0)    # E : Young Modulus (GPa)\nE.setDescription([\"Young's Modulus (GPa)\"])\n\nnu = ot.Uniform(0.3675, 0.3825)      # nu : Poisson coefficient\nnu.setDescription([\"Poisson's Ratio\"])\n\nh_c = ot.Uniform(0.0285, 0.0315)     # hc : Distance between the mean surface of the hat and the foot of the Stiffener (m)\nh_c.setDescription([\"Hat-Foot Distance (m)\"])\n\nell = ot.Uniform(0.04655, 0.05145)    # ell : Length of the stiffener flank (m)\nell.setDescription([\"Stiffener Flank Length (m)\"])\n\nf1 = ot.Uniform(0.0266, 0.0294)      # f1 : Width of the stiffener foot (m)\nf1.setDescription([\"Stiffener Foot Width (m)\"])\n\nf2 = ot.Uniform(0.00627, 0.00693)    # f2 : Width of the stiffener hat (m)\nf2.setDescription([\"Stiffener Hat Width (m)\"])\n\nt = ot.Uniform(8.019e-05, 8.181e-05)  # t : Thickness of the panel and the stiffener (m)\nt.setDescription([\"Thickness (m)\"])\n\na = ot.Uniform(0.6039, 0.6161)       # a : Width of the panel (m)\na.setDescription([\"Panel Width (m)\"])\n\nb0 = ot.Uniform(0.04455, 0.04545)    # b0 : Distance between two stiffeners (m)\nb0.setDescription([\"Inter-Stiffener Distance (m)\"])\n\np = ot.Uniform(0.03762, 0.03838)     # p : Half-width of the stiffener (m)\np.setDescription([\"Half-Stiffener Width (m)\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    E,\n    nu,\n    h_c,\n    ell,\n    f1,\n    f2,\n    t,\n    a,\n    b0,\n    p\n])\n",
    "sha256": "5bdb4b51cb9458828d18a3ee557f1709a76f80ef7747ef13f22b032a7a5bd6fa"
  },
  {
    "id": "trump_tariff",
    "title": "Tariff scenario",
    "filename": "Trump_Tariff.py",
    "domain": "Economic modelling",
    "inputDimension": 4,
    "outputDimension": 1,
    "summary": "Illustrative tariff-response model with uncertain economic assumptions.",
    "difficulty": "introductory",
    "suggestedAnalyses": [
      "monte_carlo",
      "eda",
      "sobol"
    ],
    "source": "import numpy as np\nimport openturns as ot\n###############################################################################\n# 1. Define the function_of_interest (the reciprocal tariff calculation)\n###############################################################################\ndef function_of_interest(X):\n    \"\"\"\n    Given [x, m, eps, phi],\n    compute the reciprocal tariff that drives the trade balance to zero:\n    \n      Delta_tau = (x - m) / (eps * phi * m),\n    \n    and then left-censor at zero (i.e., negative rates become zero).\n    \"\"\"\n    x, m, eps, phi = X\n    dTau = (x - m) / (eps * phi * m)\n    # Left-censor at zero\n    if dTau < 0.0:\n        dTau = 0.0\n    return [dTau]\n\n# Wrap the function into an OpenTURNS PythonFunction\nmodel = ot.PythonFunction(4, 1, function_of_interest)\n\n###############################################################################\n# 2. Define the random input variables\n###############################################################################\n\n# -- x (exports) --\n#   For illustration, assume x can range widely from 1e8 to 1e9 (units: US dollars)\nxDist = ot.Uniform(1.0e8, 1.0e9)\nxDist.setDescription([\"x\"])  # descriptive name\n\n# -- m (imports) --\n#   Similarly, let m vary from 1e8 to 1e9\nmDist = ot.Uniform(1.0e8, 1.0e9)\nmDist.setDescription([\"m\"])\n\n# -- eps (elasticity), around 4 --\n#   Use a truncated Normal distribution with mean=4, std=1, truncated to remain > 0\n#   (OpenTURNS supports ParametrizedDistribution or TruncatedDistribution)\nmean_eps = 4.0\nsigma_eps = 1.0\nepsNormal = ot.Normal(mean_eps, sigma_eps)\n# Truncate below at 0.1 just to avoid negative or near-zero elasticity\nepsDist = ot.TruncatedDistribution(epsNormal, 0.1, ot.TruncatedDistribution.LOWER)\nepsDist.setDescription([\"epsilon\"])\n\n# -- phi (pass-through), around 0.25 --\n#   Assume uniform from 0.1 to 0.4\nphiDist = ot.Uniform(0.1, 0.4)\nphiDist.setDescription([\"phi\"])\n\n###############################################################################\n# 3. Build the joint distribution\n###############################################################################\n# Assume independence among x, m, eps, phi\ninputDistributions = [xDist, mDist, epsDist, phiDist]\nproblem = ot.JointDistribution(inputDistributions)",
    "sha256": "32c565a32ca7406580e6afc1c5ae0cd761c2f63f81bd11be9ed18c11a7fba785"
  },
  {
    "id": "truss_model",
    "title": "Truss structure",
    "filename": "Truss_Model.py",
    "domain": "Structural mechanics",
    "inputDimension": 10,
    "outputDimension": 1,
    "summary": "Truss-response model combining load, geometry, and stiffness uncertainty.",
    "difficulty": "advanced",
    "suggestedAnalyses": [
      "morris",
      "monte_carlo",
      "reliability"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Truss Model Function with Scaled Units\n\ndef function_of_interest(X):\n    # Extract variables\n    E1, E2, A1, A2, P1, P2, P3, P4, P5, P6 = X\n\n    # Convert units to SI units\n    E1 = E1 * 1e9     # GPa to Pa\n    E2 = E2 * 1e9     # GPa to Pa\n    A1 = A1 * 1e-4    # cm² to m²\n    A2 = A2 * 1e-4    # cm² to m²\n    P1 = P1 * 1e3     # kN to N\n    P2 = P2 * 1e3     # kN to N\n    P3 = P3 * 1e3     # kN to N\n    P4 = P4 * 1e3     # kN to N\n    P5 = P5 * 1e3     # kN to N\n    P6 = P6 * 1e3     # kN to N\n\n    # Prevent division by zero (optional but recommended)\n    epsilon = 1e-12\n    E1 = np.maximum(E1, epsilon)\n    E2 = np.maximum(E2, epsilon)\n    A1 = np.maximum(A1, epsilon)\n    A2 = np.maximum(A2, epsilon)\n\n    # Compute the sums for the deflection formula\n    sum1 = 2 * P1 + 6 * P2 + 10 * P3 + 10 * P4 + 6 * P5 + 2 * P6\n    sum2 = 36 * P1 + 100 * P2 + 140 * P3 + 140 * P4 + 100 * P5 + 36 * P6\n\n    # Compute the midspan deflection 'y'\n    y = - (np.sqrt(2) * sum1) / (A2 * E2) - sum2 / (A1 * E1)\n\n    return [y]\n\nmodel = ot.PythonFunction(10, 1, function_of_interest)\n\n# Problem definition for the Truss Model\n# Define distributions with corrected descriptions\nE1 = ot.LogNormal()\nE1.setParameter(ot.LogNormalMuSigma()([210, 21, 0]))\nE1.setDescription([\"E1\"])\n\nE2 = ot.LogNormal()\nE2.setParameter(ot.LogNormalMuSigma()([210, 21, 0]))\nE2.setDescription([\"E2\"])\n\nA1 = ot.LogNormal()\nA1.setParameter(ot.LogNormalMuSigma()([20, 2, 0]))\nA1.setDescription([\"A1\"])\n\nA2 = ot.LogNormal()\nA2.setParameter(ot.LogNormalMuSigma()([10, 1, 0]))\nA2.setDescription([\"A2\"])\n\nP1 = ot.Gumbel(5.8477, 0.46622)  # P1 in kN\nP1.setDescription([\"P1\"])\n\nP2 = ot.Gumbel(5.8477, 0.46622)  # P2 in kN\nP2.setDescription([\"P2\"])\n\nP3 = ot.Gumbel(5.8477, 0.46622)  # P3 in kN\nP3.setDescription([\"P3\"])\n\nP4 = ot.Gumbel(5.8477, 0.46622)  # P4 in kN\nP4.setDescription([\"P4\"])\n\nP5 = ot.Gumbel(5.8477, 0.46622)  # P5 in kN\nP5.setDescription([\"P5\"])\n\nP6 = ot.Gumbel(5.8477, 0.46622)  # P6 in kN\nP6.setDescription([\"P6\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    E1,\n    E2,\n    A1,\n    A2,\n    P1,\n    P2,\n    P3,\n    P4,\n    P5,\n    P6\n])\n",
    "sha256": "4b008aff74946aeb8a7e53e668973c1b49296065450e86dd2b198bfa7c952741"
  },
  {
    "id": "tube_deflection",
    "title": "Tube deflection",
    "filename": "Tube_Deflection.py",
    "domain": "Structural mechanics",
    "inputDimension": 6,
    "outputDimension": 1,
    "summary": "Tube-deflection response under uncertain geometry, stiffness, and load.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "taylor",
      "sobol"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Deflection of a Tube Model\ndef function_of_interest(X):\n    F, L, a, De, di, E = X\n    I = np.pi * (De**4 - di**4) / 32\n    b = L - a\n    deflection = -F * a**2 * (L - a) ** 2 / (3 * E * L * I)\n    return [deflection]\n\nmodel = ot.PythonFunction(6, 1, function_of_interest)\n\n# Problem definition for the Tube Deflection Model\n# Define distributions with corrected descriptions\nF = ot.Normal(1.0, 0.1)  # Force\nF.setDescription([\"F\"])\n\nL = ot.Normal(1.5, 0.01)  # Length\nL.setDescription([\"L\"])\n\na = ot.Uniform(0.7, 1.2)  # Position of force\na.setDescription([\"a\"])\n\nDe = ot.Uniform(0.75, 0.85)  # External diameter\nDe.setDescription([\"De\"])\n\ndi = ot.Uniform(0.09, 0.11)  # Internal diameter\ndi.setDescription([\"di\"])\n\nE = ot.Normal(200000, 2000)  # Young's modulus\nE.setDescription([\"E\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    F,\n    L,\n    a,\n    De,\n    di,\n    E\n])\n",
    "sha256": "4a6f92bf88b2bace5fc96d7adfc1f67b84747065d7b16b8991d90b4d55d55403"
  },
  {
    "id": "undamped_oscillator",
    "title": "Undamped oscillator",
    "filename": "Undamped_Oscillator.py",
    "domain": "Structural dynamics",
    "inputDimension": 6,
    "outputDimension": 1,
    "summary": "Oscillator-response model without damping under uncertain forcing and structure.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "sobol",
      "reliability"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Undamped (Gayton) Oscillator Performance Function\n\ndef function_of_interest(X):\n    # Extract input variables\n    C_1, C_2, M, R, F_1, T_1 = X\n\n    # Compute natural frequency of the oscillator\n    omega_0 = np.sqrt((C_1 + C_2) / M)  # Natural frequency (rad/s)\n\n    # Compute the performance function\n    Y = 3 * R - np.abs(2 * F_1 / (M * omega_0**2) * np.sin(omega_0 * T_1 / 2))\n\n    return [Y]\n\nmodel = ot.PythonFunction(6, 1, function_of_interest)\n\n# Problem definition for the Undamped Oscillator Model\n# Define distributions with corrected descriptions\nC_1 = ot.Normal(1.0, 0.1)  # First spring constant\nC_1.setDescription([\"C_1\"])\n\nC_2 = ot.Normal(0.1, 0.01)  # Second spring constant\nC_2.setDescription([\"C_2\"])\n\nM = ot.Normal(1.0, 0.05)  # Oscillator mass\nM.setDescription([\"M\"])\n\nR = ot.Normal(0.5, 0.05)  # Yield displacement\nR.setDescription([\"R\"])\n\nF_1 = ot.Normal(0.6, 0.1)  # Applied force\nF_1.setDescription([\"F_1\"])\n\nT_1 = ot.Normal(1.0, 0.2)  # Duration of force\nT_1.setDescription([\"T_1\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    C_1,\n    C_2,\n    M,\n    R,\n    F_1,\n    T_1\n])\n",
    "sha256": "d6ebc9ef965a594df99e7e49430b020d6787fb7856e42d95f5696f43b0bece06"
  },
  {
    "id": "viscous_freefall",
    "title": "Viscous free fall",
    "filename": "Viscous_Freefall.py",
    "domain": "Fluid dynamics",
    "inputDimension": 4,
    "outputDimension": 1,
    "summary": "Falling-body response with uncertain drag, gravity, mass, and initial state.",
    "difficulty": "introductory",
    "suggestedAnalyses": [
      "monte_carlo",
      "taylor",
      "pce"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Viscous Freefall Function\ndef function_of_interest(X):\n    z0, v0, m, c = X\n    g = 9.81  # Gravitational acceleration\n    tau = m / c  # Time characteristic\n    vinf = -m * g / c  # Limit speed\n    number_of_vertices = 100\n    t = np.linspace(0.0, 12.0, number_of_vertices)\n    z = z0 + vinf * t + tau * (v0 - vinf) * (1 - np.exp(-t / tau))\n    z = np.max(z)\n    return [z]\n\nmodel = ot.PythonFunction(4, 1, function_of_interest)\n\n# Problem definition for the Viscous Freefall Model\n# Define distributions with corrected descriptions\nz0 = ot.Uniform(100.0, 150.0)  # z0\nz0.setDescription([\"z0\"])\n\nv0 = ot.Normal(55.0, 10.0)  # Initial velocity\nv0.setDescription([\"v0\"])\n\nm = ot.Normal(80.0, 8.0)  # Mass\nm.setDescription([\"m\"])\n\nc = ot.Uniform(0.0, 30.0)  # Drag coefficient\nc.setDescription([\"c\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    z0,\n    v0,\n    m,\n    c\n])\n",
    "sha256": "ff775714e8cb6678c3abd21503e397e613b75defc71d9fa246460993a57f1335"
  },
  {
    "id": "wind_turbine_power",
    "title": "Wind turbine power",
    "filename": "Wind_Turbine_Power.py",
    "domain": "Renewable energy",
    "inputDimension": 6,
    "outputDimension": 1,
    "summary": "Turbine power-response model with uncertain wind and machine parameters.",
    "difficulty": "intermediate",
    "suggestedAnalyses": [
      "monte_carlo",
      "morris",
      "gpr"
    ],
    "source": "import openturns as ot\nimport numpy as np\n\n# Wind Turbine Power Function\ndef function_of_interest(X):\n    rho_air, A, Cp, v_wind, eta, beta = X  # Air density, rotor area, power coefficient, wind speed, efficiency, blade pitch angle\n    # Adjust power coefficient based on blade pitch angle\n    Cp_adjusted = Cp * np.cos(np.radians(beta))\n    # Power extracted from wind\n    P = 0.5 * rho_air * A * Cp_adjusted * v_wind ** 3 * eta\n    return [P]\n\nmodel = ot.PythonFunction(6, 1, function_of_interest)\n\n# Problem definition for the Wind Turbine Power Model\n# Define distributions with corrected descriptions\nrho_air = ot.Uniform(1.225, 1.25)    # rho_air: Air density (kg/m³)\nrho_air.setDescription([\"rho_air\"])\n\nA = ot.Uniform(1000, 2000)           # A: Rotor swept area (m²)\nA.setDescription([\"A\"])\n\nCp = ot.Uniform(0.4, 0.5)            # Cp: Power coefficient (dimensionless)\nCp.setDescription([\"Cp\"])\n\nv_wind = ot.Normal(8, 2)  # v_wind: Wind speed (m/s)\nv_wind.setDescription([\"v_wind\"])\n\neta = ot.Uniform(0.9, 0.95)          # eta: Efficiency (dimensionless)\neta.setDescription([\"eta\"])\n\nbeta = ot.Uniform(-5, 5)             # beta: Blade pitch angle (degrees)\nbeta.setDescription([\"beta\"])\n\n# Define joint distribution (independent)\nproblem = ot.JointDistribution([\n    rho_air,\n    A,\n    Cp,\n    v_wind,\n    eta,\n    beta\n])\n",
    "sha256": "d1a8c4051360bb5ee3dfaa5e2b9c5cd56d7851fd2756e1187d51e0d0baaad991"
  }
] as const satisfies readonly ExampleCatalogEntry[];
